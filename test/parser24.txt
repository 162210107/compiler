program a;
const n:=85,a:=7;
var x,y,z,q,r;
procedure w(x,y,z);
    var a,b;
    begin
        a:=x; b:=y; z:=0;
        while b>0 do
            a:=b;
        begin
            if odd 
            then z:=z+a;
            a:=2*a; b:=b/2
        end
    end;